export enum EConstraintsX {
    left="left",
    right="right",
    center="center",
    scale="scale"
}

export enum EConstraintsY {
    top="top",
    bottom="bottom",
    center="center",
    scale="scale"
}